// checks the array elements pass the test
// does not change the original array

const arr=[2,0,92,34,11];
console.log(arr.some(i=>i%2==0))

const arr2=[3,1,9,7,77];
console.log(arr2.some(i=>i%2==0))