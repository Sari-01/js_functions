// joins the array into a string using the given seprator
// doesn't alter the original array

const arr=['sariha','sabarish'];
console.log(arr.join())
console.log(arr.join(' and '))
console.log(arr.join(';'))
