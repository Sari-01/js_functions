// pops the last element in the array
// changes the original array
// displays the removed element

const arr=['lll','ppp','ooo','kkk'];
console.log(arr.pop())

// even some object is passed it removes only the last element
console.log(arr)
console.log(arr.pop('ppp'))  // ooo is removed since it the last element now