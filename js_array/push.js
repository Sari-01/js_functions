// push the elements at the end of the array
// changes the length of the  array

const arr=['a','b','c'];
console.log(arr.push('d'))   // prints the length

console.log(arr)    // prints the array with the pushed element