// removes the white spaces 

let text="     sariha     ";
let text2=text.trim();
console.log(text)
console.log(text.trim());

// prints the string count and then the spaces after the end of the word and the remaining

console.log(text.length);
console.log(text2.length)