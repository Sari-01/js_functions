const person={
    fname:"sariha",
    lname:"raju",
    age:23,
}

console.log(person);

console.log(delete(person.age))
delete(person.age) // another way of using delete is person[age]
console.log(person)