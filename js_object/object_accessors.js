const person={
    fname:"sariha",
    lname:"raju",
    get fullname(){
        return this.fname+this.lname;
    }
}
// console.log(person.fullname)
// console.log(person.fullname())// if to use like this get should not be used (refer accessing_object_method.js)

